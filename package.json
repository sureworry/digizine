{
  "name": "digizine",
  "private": true,
  "scripts": {
    "build": "esbuild vercel-analytics-entry.mjs --bundle --format=esm --outfile=vercel-analytics.js --define:process.env.NODE_ENV=\\\"production\\\"",
    "preview": "python3 -m http.server 5173",
    "preview:node": "npx --yes serve . -l 5173 --no-clipboard",
    "browser": "bash scripts/open-in-browser.sh",
    "browser:mobile": "bash scripts/open-in-browser.sh mobile"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
